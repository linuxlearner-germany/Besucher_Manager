services:
  web:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3020:8000"
    volumes:
      - media_data:/app/media
      - static_data:/app/staticfiles

volumes:
  static_data:
  media_data:

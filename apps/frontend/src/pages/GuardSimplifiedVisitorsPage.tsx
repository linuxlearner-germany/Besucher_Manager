import { useEffect,useState } from "react";
import { AppLayout,type ApiError,fetchJson,formatDateOnly } from "../app/core";
import { Alert,Card } from "../components/ui";
type Entry={id:string;firstName:string;lastName:string;company:string;nationalityName?:string;licensePlate:string|null;barracksAreaName:string;gateName:string|null;finalValidFrom:string;finalValidUntil:string;purpose:string|null};
export function GuardSimplifiedVisitorsPage(){const [entries,setEntries]=useState<Entry[]>([]);const [search,setSearch]=useState("");const [validity,setValidity]=useState("current");const [error,setError]=useState<string|null>(null);useEffect(()=>{const p=new URLSearchParams({search,validity});void fetchJson<{entries:Entry[]}>(`/api/guard/simplified-visitors?${p}`,{method:"GET",headers:{}}).then((r)=>setEntries(r.entries)).catch((e:ApiError)=>setError(e.message||"Daten konnten nicht geladen werden."));},[search,validity]);return <AppLayout><main className="page-panel page-shell-wide"><section className="page-hero"><div className="page-hero-content"><h2>Vereinfachte Besucher</h2><p>Identität anhand des Ausweises prüfen. Kein regulärer Check-in, Check-out, Besucherschein oder Ausweishinterlegung erforderlich.</p></div></section>{error?<Alert type="error">{error}</Alert>:null}<Card><div className="toolbar"><input aria-label="Suche" placeholder="Name, Firma, Kennzeichen oder Bereich" value={search} onChange={(e)=>setSearch(e.target.value)}/><select aria-label="Gültigkeit" value={validity} onChange={(e)=>setValidity(e.target.value)}><option value="current">Aktuell gültig</option><option value="future">Zukünftig gültig</option><option value="expired">Abgelaufen</option></select></div><div className="table-wrap"><table className="data-table"><thead><tr><th>Name</th><th>Firma</th><th>Nationalität</th><th>Kennzeichen</th><th>Bereich</th><th>Wache</th><th>Gültigkeit</th><th>Tätigkeit</th></tr></thead><tbody>{entries.length?entries.map((e)=><tr key={e.id}><td><strong>{e.firstName} {e.lastName}</strong></td><td>{e.company}</td><td>{e.nationalityName||"-"}</td><td>{e.licensePlate||"-"}</td><td>{e.barracksAreaName}</td><td>{e.gateName||"Alle Wachen im Bereich"}</td><td>{formatDateOnly(e.finalValidFrom)} – {formatDateOnly(e.finalValidUntil)}</td><td>{e.purpose||"-"}</td></tr>):<tr><td colSpan={8}>Keine passenden Freigaben gefunden.</td></tr>}</tbody></table></div></Card></main></AppLayout>}
